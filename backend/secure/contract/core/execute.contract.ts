import { ReplyType } from "../../../types/.types/reply.type";


export async function executeContract(id : string) : Promise<ReplyType> {
    return {
        status : 404,
        message : "Function is not implemented",
        success : false
    }

    // USE SWITCH CASE?
}