The process of generating a new API's token (or, commonly called, the API key) goes through three different stages:
* 1 - The NASS considers that a new token is needed for the API
* 2 - The NASS generates a new token for the API and sends a request via a NASS contract
* 3 - The API asks for a new token once the contract is received
